plugins {
    id 'java'
    id 'org.example.greeting'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation(testFixtures(project(":module-testfixtures"))) {
        exclude module: 'module-testfixtures' // exclude the dependency on the main artifact
    }
}
